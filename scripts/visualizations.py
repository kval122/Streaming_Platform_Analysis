'''
Script use : For repeatable, standardized visualizations across different datasets, a plotting script can be helpful.
'''

# Imports
import matplotlib.pyplot as plt
import seaborn as sns
import plotly.express as px

# def plot_bar_chart(df, column)
# Function to create a bar chart for a specific column.

# def plot_heatmap(df)
# Function to generate a heatmap for platform content overlap.