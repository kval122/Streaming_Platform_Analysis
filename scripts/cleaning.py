'''
Script use:

Data Merging: To handle merging operations or any repeated data transformation tasks across different notebooks
'''

# Imports
import pandas as pd
import numpy as np
from sklearn.preprocessing import OneHotEncoder


# Removing duplicates.


# Splitting datasets based on availability.


# Normalizing columns for consistency.


# Cleaning genres (hot encoding or handling missing values).


#Combining platform datasets into one clean dataset.


#def clean_platform_data.df
# Function to clean each platform dataset (removes unnecessary columns, splits by availability, etc.).


#def merge_datasets(df1, df2)
# Function for merging platform data with IMDb dataset